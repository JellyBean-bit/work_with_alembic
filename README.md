# Запуск
1. Необходимо склонировать себе репозиторий. 

2. Добавить файл `.env`
   Пример файла:
   ```
    DATABASE_URL="postgresql+asyncpg://${DB_USER}:{DB_PASSWORD}@{DB_HOST}/{DB_NAME}"
   ``` 
3. В терминале находясь в корне проекта прописать:
`python main.py`

**Проект запущен!**
